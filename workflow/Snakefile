configfile: "config/config.yaml"
rule polish&select
	input:
		featurecount="counts/featurecount/raw_featurecount/{featurecounts}.txt",
		meta="Metadata/{MetaDdata}.csv"
	output:
		
	params:
		CON=config["Condition"]
	shell:
        r"""
        /path/to/my-script.R --Con {params.CON}
        """
	
rule plot:
	input:
		"Metadata/{}.csv"
	output:
	Script:
		"Scripts/Meta/plot.R"
